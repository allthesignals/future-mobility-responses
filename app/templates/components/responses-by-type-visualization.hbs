{{#each rectangles as |rectangle index|}}
  <rect class='response-rectangle'
    x={{rectangle.x}}
    y={{rectangle.y}}
    style={{generatedStyle}}
    {{action 'handleRectClick' model.q_id index}} />
{{/each}}
